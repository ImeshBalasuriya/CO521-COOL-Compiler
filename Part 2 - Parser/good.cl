class A {

    bob: String <- "hello";	(* Attribute *)
    alice: String <- "world";

    (* Method *)
    ana(a:Int, b:Int): Int {
    {
	let x:Int <- 1 in let y:Int <-5 in 2+3;
	object(5);
	alice <- "abc";
	
	if isvoid a then alice <- "Yes" else alice <- "No" fi;

	while a < 5 loop a <- a - 1 pool;

	case e of 
	    x : C => 5;
	    y: D => 7;
	esac;

	trudy <- new B;

	z <- (x + y) * ~z;


    }
    };
};

Class BB__ inherits A {
};
